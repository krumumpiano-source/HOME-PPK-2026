# 📂 เทมเพลตโฟลเดอร์และชีท — HOME PPK 2026

> **วันที่สร้าง:** 16 กุมภาพันธ์ 2569  
> **อ้างอิงจาก:** `แพลนแบคเอ็น.md` (ไฟล์ .gs 10 ไฟล์) + `Scripts/setup.gs`  
> **จุดประสงค์:**  
> 1. ระบุโฟลเดอร์ ชีท แผ่นงาน คอลัมน์ ที่จำเป็นต่อระบบทั้งหมด  
> 2. แยกหมวดหมู่ตามไฟล์ .gs ที่จะสร้าง  
> 3. เป็นแหล่งอ้างอิงในการนำ ID/ลิงก์มาพัฒนา Backend  

---

## 📌 สารบัญ

1. [โครงสร้างโฟลเดอร์ Google Drive](#-1-โครงสร้างโฟลเดอร์-google-drive)
2. [โครงสร้าง Google Sheets (6 ไฟล์ 24 แผ่นงาน)](#-2-โครงสร้าง-google-sheets)
3. [รายละเอียดคอลัมน์ทุกแผ่นงาน จำแนกตามไฟล์ .gs](#-3-รายละเอียดคอลัมน์ทุกแผ่นงาน)
4. [ตารางสรุป ID ที่ต้องบันทึก](#-4-ตารางสรุป-id-ที่ต้องบันทึก)
5. [คู่มือหลังรัน setup.gs](#-5-คู่มือหลังรัน-setupgs)

---

## 📁 1. โครงสร้างโฟลเดอร์ Google Drive

### 1.1 แผนผังโฟลเดอร์ทั้งหมด

```
HOME PPK 2026/                              ← โฟลเดอร์ราก (ROOT)
│
├── 📊 Data/                                 ← เก็บไฟล์ Google Sheets 8 ไฟล์
│   ├── [MAIN] ฐานข้อมูลหลัก.gsheet
│   ├── [WATER] ค่าน้ำ.gsheet
│   ├── [ELECTRIC] ค่าไฟ.gsheet
│   ├── [PAYMENTS] การชำระเงิน.gsheet
│   ├── [REQUESTS] คำร้อง.gsheet
│   ├── [ACCOUNTING] บัญชี.gsheet
│   ├── [WITHDRAW] สรุปเบิกจ่าย.gsheet
│   └── [NOTIFICATIONS] แจ้งยอดชำระ.gsheet
│
├── 🧾 Slips/                                ← เก็บรูปสลิปการชำระเงิน
│   └── 2569/                                ← แยกตามปี พ.ศ.
│       ├── 01-มกราคม/
│       ├── 02-กุมภาพันธ์/
│       ├── 03-มีนาคม/
│       ├── 04-เมษายน/
│       ├── 05-พฤษภาคม/
│       ├── 06-มิถุนายน/
│       ├── 07-กรกฎาคม/
│       ├── 08-สิงหาคม/
│       ├── 09-กันยายน/
│       ├── 10-ตุลาคม/
│       ├── 11-พฤศจิกายน/
│       └── 12-ธันวาคม/
│
├── 📎 RequestAttachments/                   ← เก็บไฟล์แนบคำร้อง
│   ├── ResidenceRequests/                   ← ไฟล์แนบคำร้องขอเข้าพัก
│   ├── TransferRequests/                    ← ไฟล์แนบคำร้องขอย้าย
│   ├── ReturnRequests/                      ← ไฟล์แนบคำร้องขอคืน
│   └── RepairRequests/                      ← ไฟล์แนบคำร้องแจ้งซ่อม
│
├── � AccountingReceipts/                   ← เก็บรูปหลักฐานรายรับ-รายจ่าย
│
├── �📄 Documents/                            ← เก็บเอกสาร PDF (ระเบียบ ฯลฯ)
│
├── 💾 Backups/                              ← เก็บสำเนาสำรอง (Backup_YYYYMMDD)
│
└── 📜 Scripts/                              ← เก็บไฟล์สคริปต์อ้างอิง
```

### 1.2 รายละเอียดโฟลเดอร์ + ไฟล์ .gs ที่ใช้

| # | โฟลเดอร์ | ตัวแปร ID ใน Config.gs | วัตถุประสงค์ | ไฟล์ .gs ที่ใช้ | ข้อมูลที่เก็บ |
|---|----------|----------------------|-------------|---------------|-------------|
| 1 | `HOME PPK 2026/` | `ROOT_FOLDER_ID` | โฟลเดอร์รากของระบบ | Config.gs | - |
| 2 | `Data/` | `DATA_FOLDER_ID` | เก็บ Google Sheets ทั้ง 8 ไฟล์ | Database.gs, Config.gs | Google Sheets |
| 3 | `Slips/` | `SLIPS_FOLDER_ID` | โฟลเดอร์แม่ของสลิป | Payment.gs | - |
| 4 | `Slips/2569/` | (สร้างตามปี) | สลิปปี 2569 | Payment.gs | - |
| 5 | `Slips/2569/01-มกราคม/` ถึง `12-ธันวาคม/` | (สร้างตามเดือน) | สลิปแยกรายเดือน | Payment.gs | ไฟล์รูปสลิป (.jpg/.png) |
| 6 | `RequestAttachments/` | `REQUEST_ATTACHMENTS_FOLDER_ID` | โฟลเดอร์แม่ไฟล์แนบคำร้อง | Request.gs | - |
| 7 | `RequestAttachments/ResidenceRequests/` | `RESIDENCE_REQ_FOLDER_ID` | ไฟล์แนบคำร้องเข้าพัก | Request.gs | รูป/เอกสารแนบ |
| 8 | `RequestAttachments/TransferRequests/` | `TRANSFER_REQ_FOLDER_ID` | ไฟล์แนบคำร้องย้าย | Request.gs | รูป/เอกสารแนบ |
| 9 | `RequestAttachments/ReturnRequests/` | `RETURN_REQ_FOLDER_ID` | ไฟล์แนบคำร้องคืน | Request.gs | รูป/เอกสารแนบ |
| 10 | `RequestAttachments/RepairRequests/` | `REPAIR_REQ_FOLDER_ID` | ไฟล์แนบแจ้งซ่อม | Request.gs | รูป/เอกสารแนบ |
| 11 | `AccountingReceipts/` | `ACCOUNTING_RECEIPTS_FOLDER_ID` | เก็บรูปหลักฐานบัญชี | Finance.gs | รูปใบเสร็จ/หลักฐาน (.jpg/.png) |
| 12 | `Documents/` | `DOCUMENTS_FOLDER_ID` | เอกสาร PDF ระเบียบ | Housing.gs | PDF ระเบียบบ้านพัก |
| 13 | `Backups/` | `BACKUPS_FOLDER_ID` | สำเนาสำรองอัตโนมัติ | Database.gs | สำเนา Sheets |
| 14 | `Scripts/` | `SCRIPTS_FOLDER_ID` | เก็บสคริปต์อ้างอิง | - | ไฟล์ .gs อ้างอิง |

### 1.3 การขยายโฟลเดอร์ในอนาคต

**สลิปรายปี:**
- เมื่อขึ้นปี พ.ศ. ใหม่ (เช่น 2570, 2571...) ให้เรียก `createNewYearFolders(slipsFolderId, year)`
- จะสร้างโฟลเดอร์ `Slips/2570/01-มกราคม/` ถึง `12-ธันวาคม/` อัตโนมัติ

**สำรองข้อมูล:**
- เรียก `createBackup(dataFolderId, backupFolderId)` สร้างสำเนาชื่อ `Backup_YYYYMMDD`
- ข้อมูลไม่บวมเพราะเป็น Google Sheets Link ไม่ใช่ไฟล์จริง

---

## 📊 2. โครงสร้าง Google Sheets

### 2.1 ภาพรวม (8 ไฟล์ 22+ แผ่นงาน)

| # | ชื่อไฟล์ Google Sheets | จำนวนแผ่นงาน | หมวดหมู่ | ไฟล์ .gs หลักที่ใช้ |
|---|----------------------|-------------|---------|-------------------|
| 1 | `[MAIN] ฐานข้อมูลหลัก` | 10 แผ่นงาน + `PendingReg_{ปี}` ตามปี | บ้านพัก, ผู้พัก, ผู้ใช้, สิทธิ์, ตั้งค่า, ประกาศ, Log, อัตราค่าน้ำ, ค่าส่วนกลาง, ยกเว้น, รอการสมัคร | Auth.gs, Housing.gs, Billing.gs |
| 2 | `[WATER] ค่าน้ำ` | แผ่นงานตามปี พ.ศ. (เช่น `2569`, `2570`...) | บิลค่าน้ำ | Billing.gs |
| 3 | `[ELECTRIC] ค่าไฟ` | แผ่นงานตามปี พ.ศ. (เช่น `2569`, `2570`...) | บิลค่าไฟ | Billing.gs |
| 4 | `[PAYMENTS] การชำระเงิน` | `SlipSubmissions_{ปี}` + `PaymentHistory_{ปี}` ตามปี + Outstanding (ตายตัว) | สลิป, ประวัติชำระ, ค้างชำระ | Payment.gs |
| 5 | `[REQUESTS] คำร้อง` | 4 ประเภท × แผ่นงานตามปี พ.ศ. + Queue (ตายตัว) | คำร้อง 4 ประเภท + คิว | Request.gs |
| 6 | `[ACCOUNTING] บัญชี` | แผ่นงานตามปี พ.ศ. (เช่น `2569`, `2570`...) | บัญชีรายรับ-รายจ่ายกองทุน | Finance.gs |
| 7 | `[WITHDRAW] สรุปเบิกจ่าย` | แผ่นงานตามปี พ.ศ. (เช่น `2569`, `2570`...) | สรุปเบิกจ่ายรายเดือน | Finance.gs |
| 8 | `[NOTIFICATIONS] แจ้งยอดชำระ` | แผ่นงานตามปี พ.ศ. (เช่น `2569`, `2570`...) | snapshot ข้อมูลแจ้งยอดชำระ | Billing.gs, Notification.gs |

### 2.2 เหตุผลที่แยก 8 ไฟล์

| ปัจจัย | เหตุผล |
|--------|--------|
| **ประสิทธิภาพ** | Google Sheets มีจำกัด 10 ล้านเซลล์/ไฟล์ — แยกป้องกันเกินขีด |
| **ความเร็ว** | เปิดไฟล์เล็กเร็วกว่า — ไม่ต้องโหลดข้อมูลที่ไม่เกี่ยว |
| **ความปลอดภัย** | แชร์สิทธิ์แยกกันได้ — เช่น ทีมบัญชีเข้าถึงแค่ ACCOUNTING |
| **สำรองข้อมูล** | สำรองแยกไฟล์ได้ — ไม่ต้องสำรองทั้งหมด |
| **การบำรุงรักษา** | แก้ไข/ดูแลเฉพาะส่วน — ลดความเสี่ยงแก้ผิดข้อมูลอื่น |

---

## 📋 3. รายละเอียดคอลัมน์ทุกแผ่นงาน

> **หมายเหตุ:**  
> - คอลัมน์ตำแหน่ง A, B, C... เรียงตามลำดับใน setup.gs  
> - 🔑 = Primary Key, 🔗 = Foreign Key, 📅 = วันที่/เวลา, ✅ = จำเป็น  

---

### 📄 ไฟล์ที่ 1: `[MAIN] ฐานข้อมูลหลัก`

**ตัวแปร ID:** `SPREADSHEET_IDS.MAIN`  
**ไฟล์ .gs ที่ใช้:** `Auth.gs`, `Housing.gs`, `Billing.gs`, `Database.gs`  
**วัตถุประสงค์:** เก็บข้อมูลพื้นฐานและตั้งค่าทั้งหมดของระบบ — บ้านพัก ผู้พักอาศัย บัญชีผู้ใช้ สิทธิ์ ตั้งค่า ประกาศ Log อัตราค่าน้ำ ค่าส่วนกลาง และการยกเว้น

> **หลักการ:** รวมข้อมูลตั้งค่า/พื้นฐาน (แก้ไขไม่บ่อย อ่านเยอะ) ไว้ในไฟล์เดียว — admin-settings.html เปิดแค่ไฟล์นี้ไฟล์เดียว ลด API calls

---

#### แผ่นงานที่ 1.1: `Housing` — ข้อมูลบ้านพัก

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสบ้านพัก (HOU + timestamp + random) | HOU1708012345678_A1B2 |
| B | `type` | String | ✅ | ประเภท: `house` หรือ `flat` | house |
| C | `number` | String | ✅ | หมายเลขบ้าน/ห้อง | 1-3 |
| D | `display_number` | String | ✅ | ชื่อแสดงผล (prefix+number) | บ้าน 1-3 |
| E | `zone` | String | | โซน/พื้นที่ | A |
| F | `status` | String | ✅ | สถานะ: `available`/`occupied`/`maintenance` | occupied |
| G | `note` | String | | หมายเหตุ | อยู่ใกล้ประตูทางเข้า |
| H | `created_at` 📅 | DateTime | ✅ | วันที่สร้าง | 2569-02-15 10:00:00 |
| I | `updated_at` 📅 | DateTime | | วันที่แก้ไขล่าสุด | 2569-02-16 14:30:00 |

> **หมายเหตุ HTML:** `admin-settings.html` สร้างข้อมูลรูปแบบ `{ id, type, number, displayNumber, zone, status, note, createdAt }` — ค่า status ใช้ `available` (ไม่ใช่ `vacant`)

**ไฟล์ .gs ที่ใช้:** `Housing.gs` — `getHousingList()`, `addHousing()`, `updateHousing()`, `deleteHousing()`, `getAvailableHousing()`  
**HTML ที่เรียก:** `admin-settings.html`, `transfer-form.html`, `check-request.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~50-100 แถว (จำนวนบ้าน/ห้องทั้งหมด)

---

#### แผ่นงานที่ 1.2: `Residents` — ผู้พักอาศัย

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสผู้พักอาศัย | RES1708012345678_C3D4 |
| B | `resident_type` | String | ✅ | ประเภท: `staff` (บุคลากร) / `cohabitant` (ผู้ร่วมพัก) | staff |
| C | `prefix` | String | ✅ | คำนำหน้าชื่อ: นาย/นาง/นางสาว/อื่นๆ | นาย |
| D | `firstname` | String | ✅ | ชื่อ | สมชาย |
| E | `lastname` | String | ✅ | นามสกุล | ใจดี |
| F | `position` | String | | ตำแหน่ง / กลุ่มสาระ | วิทยาศาสตร์และเทคโนโลยี |
| G | `subject_group` | String | | กลุ่มสาระ (settings.html ใช้แยกจาก position) | วิทยาศาสตร์และเทคโนโลยี |
| H | `phone` | String | ✅ | เบอร์โทร | 081-234-5678 |
| I | `email` | String | ✅ | อีเมล | somchai@email.com |
| J | `house_number` 🔗 | String | ✅ | เลขบ้านที่อยู่ (→ Housing.display_number) | บ้าน 1-3 |
| K | `address_no` | String | | บ้านเลขที่ (ที่อยู่ตามทะเบียนบ้าน) | 123/4 |
| L | `address_road` | String | | ถนน | ซุปเปอร์ไฮเวย์ |
| M | `address_village` | String | | หมู่บ้าน/หมู่ | 5 |
| N | `subdistrict` | String | | ตำบล/แขวง | แม่ต๋ำ |
| O | `district` | String | | อำเภอ/เขต | เมืองพะเยา |
| P | `province` | String | | จังหวัด | พะเยา |
| Q | `zipcode` | String | | รหัสไปรษณีย์ | 56000 |
| R | `move_in_date` 📅 | Date | | วันที่ย้ายเข้า | 2569-01-15 |
| S | `cohabitants` | Number | | จำนวนผู้ร่วมพัก | 2 |
| T | `cohabitant_names` | String | | ชื่อผู้ร่วมพัก (JSON array) | [{"name":"สมหญิง","relation":"spouse"}] |
| U | `profile_photo` | String | | รูปโปรไฟล์ base64 หรือ Drive file ID | data:image/jpeg;base64,... |
| V | `status` | String | ✅ | สถานะ: `active` / `inactive` | active |
| W | `created_at` 📅 | DateTime | ✅ | วันที่สร้าง | 2569-02-15 10:00:00 |
| X | `updated_at` 📅 | DateTime | | วันที่แก้ไขล่าสุด | 2569-02-16 14:30:00 |

> **หมายเหตุ HTML:**
> - `register.html` ใช้ `position` (select) สำหรับค่ากลุ่มสาระ: ภาษาไทย, คณิตศาสตร์, วิทยาศาสตร์ฯ, สังคมศึกษาฯ, สุขศึกษาฯ, ศิลปะ, การงานอาชีพ, ภาษาต่างประเทศ, เจ้าหน้าที่, ลูกจ้าง
> - `settings.html` ใช้ทั้ง `position` + `subject_group` แยกกัน — Backend ต้องรองรับทั้งสอง
> - `admin-settings.html` เก็บรูปแบบ `{ id, residentType: 'staff'|'cohabitant', prefix, firstname, lastname, position, phone, house_number, email, password, cohabitants, cohabitantNames, status }` — Backend ต้อง normalize password ไปเก็บใน Users sheet แทน
> - `settings.html` เก็บ `profilePhoto` เป็น base64 ใน localStorage — Backend อาจบันทึกลง Drive แล้วเก็บ file ID แทน
> - ที่อยู่ 7 ฟิลด์ (K-Q) มาจาก `register.html` + `settings.html` — เป็นที่อยู่ตามทะเบียนบ้าน ไม่ใช่ที่อยู่บ้านพักครู
> - ผู้ร่วมพักของ `settings.html` ใช้ status: `spouse`/`child`/`father`/`mother`

**ไฟล์ .gs ที่ใช้:** `Housing.gs` — `getResidentsList()`, `addResident()`, `updateResident()`, `moveResident()`, `removeResident()`, `getCoresidents()`, `addCoresident()`, `updateCoresident()`, `removeCoresident()`  
**HTML ที่เรียก:** `admin-settings.html`, `settings.html`, `register.html`, `record-water.html`, `record-electric.html`, `check-slip.html`, `payment-notification.html`, `accounting.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~100-300 แถว (ผู้พักหลัก + ผู้ร่วมพัก)

---

#### แผ่นงานที่ 1.3: `Users` — บัญชีผู้ใช้

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสผู้ใช้ | USR1708012345678_E5F6 |
| B | `email` | String | ✅ | อีเมลเข้าสู่ระบบ (unique) | somchai@email.com |
| C | `phone` | String | ✅ | เบอร์โทร (ใช้ค้นหาใน forgot-email) | 081-234-5678 |
| D | `password_hash` | String | ✅ | รหัสผ่านเข้ารหัส SHA-256 | a1b2c3d4e5... |
| E | `resident_id` 🔗 | String | ✅ | รหัสผู้พักอาศัย (→ Residents.id) | RES1708012345678_C3D4 |
| F | `role` | String | ✅ | บทบาท: `user` / `team` / `admin` | user |
| G | `is_active` | Boolean | ✅ | สถานะบัญชี: TRUE/FALSE | TRUE |
| H | `pdpa_consent` | Boolean | ✅ | ยินยอม PDPA: TRUE/FALSE | TRUE |
| I | `last_login` 📅 | DateTime | | เข้าสู่ระบบล่าสุด | 2569-02-16 08:00:00 |
| J | `created_at` 📅 | DateTime | ✅ | วันที่สมัคร | 2569-02-15 10:00:00 |

> **หมายเหตุ HTML:**
> - `register.html` มีฟิลด์ `pdpaConsent` (checkbox ยินยอม PDPA) — ต้องเก็บลง Users sheet
> - `register.html` มีฟิลด์ `phone` — ต้องเก็บเพื่อให้ `forgot-email.html` ค้นหาได้
> - `admin-settings.html` เก็บ `password` (btoa base64) ไว้ใน residentsData — Backend ต้อง normalize มาเก็บเป็น SHA-256 hash ใน Users sheet แทน
> - `forgot-email.html` ค้นหาอีเมลจากเบอร์โทร (search by phone) — ต้องมีคอลัมน์ phone ใน Users

**ไฟล์ .gs ที่ใช้:** `Auth.gs` — `handleLogin()`, `handleRegister()`, `handleResetPassword()`, `handleFindEmail()`, `handleChangePassword()`, `getCurrentUser()`  
**HTML ที่เรียก:** `login.html`, `register.html`, `forgot-password.html`, `forgot-email.html`, `settings.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~50-200 แถว

---

#### แผ่นงานที่ 1.4: `Permissions` — สิทธิ์การจัดการ

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `user_id` 🔗 | String | ✅ | รหัสผู้ใช้ (→ Users.id) | USR1708012345678_E5F6 |
| B | `water` 💧 | Boolean | ✅ | สิทธิ์บันทึกค่าน้ำ | TRUE |
| C | `electric` ⚡ | Boolean | ✅ | สิทธิ์บันทึกค่าไฟ | FALSE |
| D | `notify` 📢 | Boolean | ✅ | สิทธิ์แจ้งยอด | TRUE |
| E | `slip` 🧾 | Boolean | ✅ | สิทธิ์ตรวจสลิป | FALSE |
| F | `withdraw` 💰 | Boolean | ✅ | สิทธิ์สรุปเบิกจ่าย | FALSE |
| G | `accounting` 📑 | Boolean | ✅ | สิทธิ์บัญชี | FALSE |
| H | `request` 📋 | Boolean | ✅ | สิทธิ์ตรวจคำร้อง | FALSE |
| I | `admin` 🔧 | Boolean | ✅ | สิทธิ์ผู้ดูแลระบบ | FALSE |
| J | `updated_at` 📅 | DateTime | | วันที่แก้ไขล่าสุด | 2569-02-16 14:30:00 |
| K | `updated_by` | String | | ผู้แก้ไข | admin@ppk.ac.th |

**ไฟล์ .gs ที่ใช้:** `Auth.gs` — `checkPermission()` / `Housing.gs` — `getPermissions()`, `updatePermissions()`  
**HTML ที่เรียก:** `admin-settings.html` (แท็บ Permissions), `team-management.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~10-30 แถว (เฉพาะ team/admin)

> **หมายเหตุ HTML:** `admin-settings.html` กำหนด permission types: `water`, `electric`, `notify`, `slip`, `withdraw`, `accounting`, `request`, `admin` — ตรงกับคอลัมน์ B-I

---

#### แผ่นงานที่ 1.5: `Settings` — ค่าตั้งระบบ

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `key` 🔑 | String | ✅ | ชื่อค่าตั้ง (unique) | water_rate |
| B | `value` | String | ✅ | ค่า | 5 |
| C | `description` | String | | คำอธิบาย | อัตราค่าน้ำ (บาท/หน่วย) |
| D | `updated_at` 📅 | DateTime | | วันที่แก้ไขล่าสุด | 2569-02-16 14:30:00 |
| E | `updated_by` | String | | ผู้แก้ไข | admin@ppk.ac.th |

**ค่าเริ่มต้น 46 รายการ:** (สร้างอัตโนมัติโดย setup.gs v2.0 — sync กับ `แก้ไขตามนี้โดยด่วน.md` §2)

| กลุ่ม | key | value | description |
|-------|-----|-------|-------------|
| 1. ค่าพื้นฐาน | org_name | งานส่งเสริม กำกับ ดูแล และพัฒนาบ้านพักครู | ชื่อหน่วยงาน |
| | school_name | โรงเรียนพะเยาพิทยาคม | ชื่อโรงเรียน |
| | admin_email | (ว่าง) | อีเมลผู้ดูแลระบบ |
| | admin_phone | (ว่าง) | เบอร์โทรผู้ดูแลระบบ |
| | water_rate | ⚠️ **(ว่าง — แอดมินกำหนดเท่านั้น)** | อัตราค่าน้ำ (บาท/หน่วย) |
| | common_fee_house | 110 | ค่าส่วนกลางบ้านพัก (บาท/เดือน) |
| | common_fee_flat | 110 | ค่าส่วนกลางแฟลต (บาท/เดือน) |
| | garbage_fee | 310 | ค่าขยะ (บาท/เดือน) |
| | due_date | 15 | วันกำหนดชำระ |
| | reminder_days | 5 | จำนวนวันก่อนครบกำหนดที่ส่งเตือน |
| | house_prefix | บ้าน | คำนำหน้าบ้าน |
| | flat_prefix | แฟลต | คำนำหน้าแฟลต |
| 2. ค่าไฟ | electric_method | bill | วิธีบันทึก: bill (ตามบิล) / unit (ตามหน่วย) |
| | electric_rate | ⚠️ **(ว่าง — ใช้เมื่อ method=unit)** | อัตราค่าไฟ (บาท/หน่วย) |
| | electric_min_charge | 0 | ค่าไฟขั้นต่ำ |
| | electric_rounding | ceil | การปัดเศษ: none/round/ceil/floor |
| 3. ค่าน้ำเพิ่มเติม | water_min_charge | 0 | ค่าน้ำขั้นต่ำ |
| | water_rounding | none | การปัดเศษค่าน้ำ |
| 4. รูปแบบเลขที่ | house_number_format | {prefix} {number} | รูปแบบเลขบ้านพัก |
| | flat_number_format | {prefix} {number} | รูปแบบเลขแฟลต |
| 5. ระบบ | require_login | true | บังคับเข้าสู่ระบบ |
| | allow_reset_password | true | อนุญาตรีเซ็ตรหัสผ่าน |
| | allow_registration | true | เปิดรับสมัครสมาชิก |
| | queue_expiry_days | 180 | จำนวนวันหมดอายุคิว |
| 6. เจ้าหน้าที่ลงนาม | meter_recorder | (ว่าง) | ชื่อผู้บันทึกมิเตอร์ |
| | meter_checker | (ว่าง) | ชื่อผู้ตรวจสอบมิเตอร์ |
| | head_of_promotion | (ว่าง) | ชื่อหัวหน้างานส่งเสริมฯ |
| | vice_director | (ว่าง) | ชื่อรองผู้อำนวยการ |
| | director | (ว่าง) | ชื่อผู้อำนวยการ |
| 7. อีเมล | email_sender | (ว่าง) | ชื่อผู้ส่งอีเมล |
| | email_signature | (ว่าง) | ลายเซ็นท้ายอีเมล |
| | email_reminder_note | (ว่าง) | ข้อความเพิ่มเติมในอีเมลแจ้งเตือน |
| | email_receipt_note | (ว่าง) | ข้อความเพิ่มเติมในอีเมลใบเสร็จ |
| 8. Checkbox อีเมลแจ้งยอด | reminder_include_water | true | แสดงค่าน้ำ |
| | reminder_include_electric | true | แสดงค่าไฟ |
| | reminder_include_common | true | แสดงค่าส่วนกลาง |
| | reminder_include_total | true | แสดงยอดรวม |
| | reminder_include_due | true | แสดงวันครบกำหนด |
| | reminder_include_meter | true | แสดงเลขมิเตอร์ |
| | reminder_include_qr | false | แสดง QR Code |
| 8. Checkbox อีเมลใบเสร็จ | receipt_include_water | true | แสดงค่าน้ำ |
| | receipt_include_electric | true | แสดงค่าไฟ |
| | receipt_include_common | true | แสดงค่าส่วนกลาง |
| | receipt_include_total | true | แสดงยอดรวม |
| | receipt_include_paid | true | แสดงยอดที่ชำระ |
| | receipt_include_date | true | แสดงวันที่ชำระ |

> **⚠️ หมายเหตุสำคัญ:**
> - `water_rate` = ว่าง — **แอดมินต้องตั้งค่าอัตราค่าน้ำผ่าน admin-settings.html ก่อนใช้งานระบบ** (WaterRates sheet ก็ว่าง รอแอดมินกำหนด)
> - `electric_rate` = ว่าง — ถูกต้องตามการออกแบบเพราะค่าเริ่มต้น `electric_method=bill` (บันทึกตามบิล กฟภ.) ไม่ต้องใช้อัตราต่อหน่วย / ตั้งค่าเมื่อเปลี่ยนเป็น `method=unit` เท่านั้น
> - `admin-settings.html` ยังเขียน localStorage key แยก: `waterRate`, `commonFee`, `adminEmail`, `due_date_working_days`, `queueExpiryDate`, `announcements`, `regulations_pdf`

**ไฟล์ .gs ที่ใช้:** `Housing.gs` — `getSettings()`, `handleUpdateSettings()`  
**HTML ที่เรียก:** `admin-settings.html` (แท็บ System), `dashboard.html`, `record-water.html`, `payment-notification.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~46 แถว (ตั้งต้น) + เพิ่มในอนาคต

---

#### แผ่นงานที่ 1.6: `Announcements` — ประกาศ

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสประกาศ | ANN1708012345678_G7H8 |
| B | `text` | String | ✅ | เนื้อหาประกาศ | แจ้งชำระค่าน้ำ-ไฟ ภายใน 15 ก.พ. |
| C | `priority` | String | ✅ | ระดับ: `normal` / `important` / `urgent` | important |
| D | `expiry_date` 📅 | Date | | วันหมดอายุ | 2569-02-28 |
| E | `is_active` | Boolean | ✅ | สถานะ: TRUE/FALSE | TRUE |
| F | `created_by` | String | | ผู้สร้าง | admin@ppk.ac.th |
| G | `created_at` 📅 | DateTime | ✅ | วันที่สร้าง | 2569-02-15 10:00:00 |

**ไฟล์ .gs ที่ใช้:** `Housing.gs` — `getAnnouncements()`, `handleAddAnnouncement()`, `deleteAnnouncement()`  
**HTML ที่เรียก:** `admin-settings.html` (แท็บ Announcements), `dashboard.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~10-50 แถว

---

#### แผ่นงานที่ 1.7: `Logs` — บันทึกกิจกรรม

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัส Log | LOG1708012345678_I9J0 |
| B | `timestamp` 📅 | DateTime | ✅ | เวลาที่เกิดเหตุ | 2569-02-16 14:30:00 |
| C | `user_email` | String | ✅ | อีเมลผู้กระทำ | somchai@email.com |
| D | `action` | String | ✅ | การกระทำ | LOGIN / SAVE_WATER_BILL / APPROVE_SLIP |
| E | `module` | String | ✅ | โมดูล | Auth / Billing / Payment |
| F | `details` | String | | รายละเอียดเพิ่มเติม (JSON) | {"period":"2569-02","house":"บ้าน 1-3"} |
| G | `ip_address` | String | | IP Address (ถ้ามี) | 203.150.xxx.xxx |

**ไฟล์ .gs ที่ใช้:** `Database.gs` — `writeLog()` (ถูกเรียกจากทุกไฟล์ .gs)  
**HTML ที่เรียก:** ไม่มี HTML เรียกตรง (เป็น background log)  
**ปริมาณข้อมูลโดยประมาณ:** ~1,000-10,000+ แถว/ปี (เพิ่มทุกครั้งที่มีการกระทำ)

⚠️ **คำแนะนำ:** ควรตั้งระบบ Archive Log เก่า > 6 เดือน ไปไฟล์แยก เพื่อไม่ให้ sheet ช้า

---

#### แผ่นงานที่ 1.8: `WaterRates` — อัตราค่าน้ำ

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|----------|
| A | `id` 🔑 | String | ✅ | รหัสอัตรา | RAT1708012345678_O5P6 |
| B | `min_units` | Number | ✅ | หน่วยเริ่มต้น | 0 |
| C | `max_units` | Number | ✅ | หน่วยสิ้นสุด (999999 = ไม่จำกัด) | 999999 |
| D | `rate` | Number | ✅ | อัตรา (บาท/หน่วย) | 5 |
| E | `effective_date` 📅 | Date | ✅ | วันที่มีผล | 2569-01-01 |
| F | `created_at` 📅 | DateTime | ✅ | วันที่สร้าง | 2569-02-15 10:00:00 |
| G | `created_by` | String | | ผู้สร้าง | admin@ppk.ac.th |

**ไฟล์ .gs ที่ใช้:** `Billing.gs` — `getWaterRate()`, `updateWaterRate()`  
**HTML ที่เรียก:** `admin-settings.html` (แท็บ Water), `record-water.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~1-5 แถว (อัตราปัจจุบัน + ประวัติ)

---

#### แผ่นงานที่ 1.9: `CommonFee` — ค่าส่วนกลาง

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|----------|
| A | `id` 🔑 | String | ✅ | รหัส | CMF1708012345678_Q7R8 |
| B | `type` | String | ✅ | ประเภท: `house` / `flat` | house |
| C | `amount` | Number | ✅ | ค่าส่วนกลาง (บาท/เดือน) | 110 |
| D | `effective_date` 📅 | Date | ✅ | วันที่มีผล | 2569-01-01 |
| E | `created_at` 📅 | DateTime | ✅ | วันที่สร้าง | 2569-02-15 10:00:00 |
| F | `created_by` | String | | ผู้สร้าง | admin@ppk.ac.th |

**ไฟล์ .gs ที่ใช้:** `Billing.gs` — `getCommonFee()`, `updateCommonFee()`  
**HTML ที่เรียก:** `admin-settings.html` (แท็บ CommonFee), `upload-slip.html`, `check-slip.html`, `payment-notification.html`, `accounting.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~2-10 แถว (อัตราปัจจุบันบ้าน + แฟลต + ประวัติ)

> **⚠️ ข้อบกพร่องใน HTML:** `admin-settings.html` เขียน localStorage key `commonFee` แต่ `upload-slip.html` + `accounting.html` อ่าน key `commonFeeRate` — Backend ต้อง return ค่า common fee ผ่าน API ให้ทุกหน้าใช้ค่าเดียวกัน

---

#### แผ่นงานที่ 1.10: `Exemptions` — การยกเว้น

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|----------|
| A | `id` 🔑 | String | ✅ | รหัส | EXM1708012345678_S9T0 |
| B | `house_number` 🔗 | String | ✅ | เลขบ้าน (→ Housing.display_number) | บ้าน 2-1 |
| C | `exemption_type` | String | ✅ | ประเภท: `water` / `electric` / `common_fee` / `all` | common_fee |
| D | `reason` | String | ✅ | เหตุผล | บ้านว่าง ไม่มีผู้พักอาศัย |
| E | `start_date` 📅 | Date | ✅ | วันเริ่มยกเว้น | 2569-01-01 |
| F | `end_date` 📅 | Date | | วันสิ้นสุดยกเว้น (ว่าง = ไม่มีกำหนด) | 2569-06-30 |
| G | `created_at` 📅 | DateTime | ✅ | วันที่สร้าง | 2569-02-15 10:00:00 |
| H | `created_by` | String | | ผู้สร้าง | admin@ppk.ac.th |

**ไฟล์ .gs ที่ใช้:** `Billing.gs` — `getExemptions()`, `updateExemptions()`  
**HTML ที่เรียก:** `admin-settings.html` (แท็บ Exemptions)  
**ปริมาณข้อมูลโดยประมาณ:** ~0-20 แถว

---

#### แผ่นงานตามปี: `PendingReg_{ปี พ.ศ.}` เช่น `PendingReg_2569` — รอการอนุมัติสมัครสมาชิก

> **หมายเหตุ:** แผ่นงานนี้อยู่ในไฟล์ [MAIN] เพราะ Users + Residents อยู่ในไฟล์เดียวกัน — เมื่ออนุมัติจะย้ายข้อมูลภายในไฟล์เดียวกัน ลด API calls

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสการสมัคร | REG1708012345678_A1B2 |
| B | `email` | String | ✅ | อีเมล | somchai@email.com |
| C | `phone` | String | ✅ | เบอร์โทร | 081-234-5678 |
| D | `prefix` | String | ✅ | คำนำหน้า: นาย/นาง/นางสาว/อื่นๆ | นาย |
| E | `firstname` | String | ✅ | ชื่อ | สมชาย |
| F | `lastname` | String | ✅ | นามสกุล | ใจดี |
| G | `position` | String | ✅ | กลุ่มสาระ / ตำแหน่ง | วิทยาศาสตร์และเทคโนโลยี |
| H | `address_no` | String | ✅ | บ้านเลขที่ | 123/4 |
| I | `address_road` | String | | ถนน | ซุปเปอร์ไฮเวย์ |
| J | `address_village` | String | | หมู่บ้าน/หมู่ | 5 |
| K | `subdistrict` | String | ✅ | ตำบล/แขวง | แม่ต๋ำ |
| L | `district` | String | ✅ | อำเภอ/เขต | เมืองพะเยา |
| M | `province` | String | ✅ | จังหวัด | พะเยา |
| N | `zipcode` | String | ✅ | รหัสไปรษณีย์ (5 หลัก) | 56000 |
| O | `password_hash` | String | ✅ | รหัสผ่านเข้ารหัส SHA-256 | a1b2c3d4e5... |
| P | `pdpa_consent` | Boolean | ✅ | ยินยอม PDPA: TRUE/FALSE | TRUE |
| Q | `status` | String | ✅ | สถานะ: `pending` / `approved` / `rejected` | pending |
| R | `reviewed_by` | String | | ผู้ตรวจ (admin) | admin@ppk.ac.th |
| S | `reviewed_at` 📅 | DateTime | | วันที่ตรวจ | 2569-02-16 14:30:00 |
| T | `review_note` | String | | หมายเหตุการตรวจ | อนุมัติ กำหนดบ้าน 1-3 |
| U | `submitted_at` 📅 | DateTime | ✅ | วันที่สมัคร | 2569-02-15 10:00:00 |

**การสร้างแผ่นงานปีใหม่อัตโนมัติ:**
- เมื่อเรียก `handleRegister(data)` แล้วไม่พบแผ่นงาน `PendingReg_{ปี}` → สร้างแผ่นงานใหม่พร้อม Header อัตโนมัติ

**เมื่อแอดมินอนุมัติ:**
1. อัปเดต `status` → `approved` ใน PendingReg sheet
2. สร้างข้อมูลใน `Users` sheet (id, email, phone, password_hash, role='user', is_active=TRUE, pdpa_consent)
3. สร้างข้อมูลใน `Residents` sheet (id, resident_type='staff', prefix, firstname, lastname, position, phone, email, address, status='active')
4. ทั้งหมดเกิดในไฟล์ [MAIN] เดียวกัน — 1 API call

> **หมายเหตุ HTML:** `register.html` ปัจจุบัน `validateRegister()` จบด้วย `return false` — ต้องเพิ่มการเรียก Backend + แสดงข้อความ "รอแอดมินอนุมัติ"  
> `admin-settings.html` ต้องเพิ่มแท็บ/ส่วน "อนุมัติการสมัคร" — ดึงรายการ pending → อนุมัติ/ปฏิเสธ

**ไฟล์ .gs ที่ใช้:** `Auth.gs` — `handleRegister()`, `getPendingRegistrations()`, `approveRegistration()`, `rejectRegistration()`  
**HTML ที่เรียก:** `register.html`, `admin-settings.html` (แท็บอนุมัติการสมัคร)  
**ปริมาณข้อมูลโดยประมาณ:** ~5-30 แถว/ปี (ลดลงเมื่ออนุมัติ/ปฏิเสธ ข้อมูลยังคงอยู่เพื่อ audit)

---

### 📄 ไฟล์ที่ 2: `[WATER] ค่าน้ำ`

**ตัวแปร ID:** `SPREADSHEET_IDS.WATER`  
**ไฟล์ .gs ที่ใช้:** `Billing.gs`  
**วัตถุประสงค์:** เก็บข้อมูลบิลค่าน้ำรายเดือน (ข้อมูล transactional ที่เพิ่มทุกเดือน) — แผ่นงานแยกตามปี พ.ศ.

> **หลักการ:**
> 1. แยกออกจากค่าไฟเป็นไฟล์ต่างหาก — แชร์สิทธิ์/สำรองอิสระ
> 2. แผ่นงานแยกตามปี พ.ศ. (เช่น `2569`, `2570`...) — สร้างอัตโนมัติเมื่อขึ้นปีใหม่
> 3. อัตราค่าน้ำ (WaterRates) อยู่ใน [MAIN] — ไม่อยู่ในไฟล์นี้

---

#### แผ่นงานตามปี: `{ปี พ.ศ.}` เช่น `2569` — บิลค่าน้ำประจำปี

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสบิลน้ำ | WTR1708012345678_K1L2 |
| B | `month` | Number | ✅ | เดือน (1-12) | 2 |
| C | `house_number` 🔗 | String | ✅ | เลขบ้าน (→ Housing.display_number) | บ้าน 1-3 |
| D | `resident_name` | String | | ชื่อผู้พัก (denormalize เพื่อความเร็ว) | สมชาย ใจดี |
| E | `prev_meter` | Number | ✅ | เลขมิเตอร์ก่อน | 1250 |
| F | `curr_meter` | Number | ✅ | เลขมิเตอร์ปัจจุบัน | 1275 |
| G | `units` | Number | ✅ | จำนวนหน่วย (curr - prev) | 25 |
| H | `rate` | Number | ✅ | อัตราค่าน้ำ (บาท/หน่วย) | 5 |
| I | `amount` | Number | ✅ | ยอด (units × rate) | 125 |
| J | `saved_at` 📅 | DateTime | ✅ | วันที่บันทึก | 2569-02-01 09:00:00 |
| K | `saved_by` | String | | ผู้บันทึก | team@ppk.ac.th |

**การสร้างแผ่นงานปีใหม่อัตโนมัติ:**
- เมื่อเรียก `getWaterBills(period)` หรือ `saveWaterBill(period, records)` แล้วไม่พบแผ่นงานของปีนั้น → เรียก `createWaterYearSheet(year)` สร้างแผ่นงานใหม่พร้อม Header อัตโนมัติ

**ไฟล์ .gs ที่ใช้:** `Billing.gs` — `getWaterBills()`, `saveWaterBill()`, `getPreviousWaterMeter()`, `calculateWaterAmount()`, `createWaterYearSheet()`  
**HTML ที่เรียก:** `record-water.html`, `upload-slip.html`, `check-slip.html`, `payment-notification.html`, `monthly-withdraw.html`, `accounting.html`, `dashboard.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~50-100 แถว/เดือน × 12 เดือน = ~600-1,200 แถว/ปี (ต่อแผ่นงาน)

> **หมายเหตุ HTML:** `record-water.html` บันทึกรูปแบบ `{ month, year, rate, records: [{ house, resident, prevMeter, currMeter, usage, amount }], savedAt }` — ค่าเริ่มต้น `water_rate` = 18 (จาก HTML) แต่ Settings ตั้งเป็น 5 — Frontend อ่านจาก `waterRate` localStorage key ที่ `admin-settings.html` เขียน

> **หมายเหตุ:** คอลัมน์ `year` ถูกตัดออกเนื่องจากแผ่นงานแยกตามปีแล้ว — ปีของข้อมูลระบุโดยชื่อแผ่นงาน (เช่น `2569`)

---

### 📄 ไฟล์ที่ 3: `[ELECTRIC] ค่าไฟ`

**ตัวแปร ID:** `SPREADSHEET_IDS.ELECTRIC`  
**ไฟล์ .gs ที่ใช้:** `Billing.gs`  
**วัตถุประสงค์:** เก็บข้อมูลบิลค่าไฟรายเดือน (ข้อมูล transactional ที่เพิ่มทุกเดือน) — แผ่นงานแยกตามปี พ.ศ.

> **หลักการ:**
> 1. แยกออกจากค่าน้ำเป็นไฟล์ต่างหาก — แชร์สิทธิ์/สำรองอิสระ
> 2. แผ่นงานแยกตามปี พ.ศ. (เช่น `2569`, `2570`...) — สร้างอัตโนมัติเมื่อขึ้นปีใหม่

---

#### แผ่นงานตามปี: `{ปี พ.ศ.}` เช่น `2569` — บิลค่าไฟประจำปี

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสบิลไฟ | ELC1708012345678_M3N4 |
| B | `month` | Number | ✅ | เดือน (1-12) | 2 |
| C | `house_number` 🔗 | String | ✅ | เลขบ้าน (→ Housing.display_number) | บ้าน 1-3 |
| D | `resident_name` | String | | ชื่อผู้พัก | สมชาย ใจดี |
| E | `amount` | Number | ✅ | ยอดค่าไฟ (บาท) | 450.50 |
| F | `pea_total` | Number | | ยอดรวม กฟภ. ทั้งเดือน (ซ้ำทุกแถวในเดือนเดียวกัน) | 45000 |
| G | `lost_house` | Number | | ค่าสูญเสียบ้านพัก (บาท) | 500 |
| H | `lost_flat` | Number | | ค่าสูญเสียแฟลต (บาท) | 300 |
| I | `saved_at` 📅 | DateTime | ✅ | วันที่บันทึก | 2569-02-01 09:30:00 |
| J | `saved_by` | String | | ผู้บันทึก | team@ppk.ac.th |

**การสร้างแผ่นงานปีใหม่อัตโนมัติ:**
- เมื่อเรียก `getElectricBills(period)` หรือ `saveElectricBill(period, ...)` แล้วไม่พบแผ่นงานของปีนั้น → เรียก `createElectricYearSheet(year)` สร้างแผ่นงานใหม่พร้อม Header อัตโนมัติ

> **หมายเหตุ HTML:** `record-electric.html` บันทึกข้อมูลรูปแบบ `{ month, year, pea_total, lost_house, lost_flat, records: [{ house, resident, amount }], savedAt }` — คอลัมน์ F-H เป็นค่าระดับเดือน (period-level) ที่ซ้ำกันทุกแถวในเดือนเดียวกัน, ใช้ `Math.ceil()` ในการปัดเศษ

> **หมายเหตุ:** คอลัมน์ `year` ถูกตัดออกเนื่องจากแผ่นงานแยกตามปีแล้ว — ปีของข้อมูลระบุโดยชื่อแผ่นงาน (เช่น `2569`)

**ไฟล์ .gs ที่ใช้:** `Billing.gs` — `getElectricBills()`, `saveElectricBill()`, `createElectricYearSheet()`  
**HTML ที่เรียก:** `record-electric.html`, `upload-slip.html`, `check-slip.html`, `payment-notification.html`, `monthly-withdraw.html`, `accounting.html`, `dashboard.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~50-100 แถว/เดือน × 12 = ~600-1,200 แถว/ปี (ต่อแผ่นงาน)

---

### 📄 ไฟล์ที่ 4: `[NOTIFICATIONS] แจ้งยอดชำระ`

**ตัวแปร ID:** `SPREADSHEET_IDS.NOTIFICATIONS`  
**ไฟล์ .gs ที่ใช้:** `Billing.gs`, `Notification.gs`  
**วัตถุประสงค์:** เก็บ snapshot ข้อมูลแจ้งยอดชำระที่บันทึกแล้ว เพื่อเรียกดูย้อนหลัง — แผ่นงานแยกตามปี พ.ศ.

> **หลักการ:**
> 1. เป็น snapshot ของข้อมูลที่ "ส่งแจ้งไปแล้ว" — ไม่ใช่ข้อมูลบิลดิบ
> 2. แผ่นงานแยกตามปี พ.ศ. (เช่น `2569`, `2570`...) — สร้างอัตโนมัติเมื่อขึ้นปีใหม่
> 3. บันทึกทุกครั้งที่กดบันทึกรูปในหน้า payment-notification.html
> 4. แชร์สิทธิ์/สำรองอิสระ

---

#### แผ่นงานตามปี: `{ปี พ.ศ.}` เช่น `2569` — ข้อมูลแจ้งยอดชำระประจำปี

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัส snapshot | NTF1708012345678_A1B2 |
| B | `month` | Number | ✅ | เดือน (1-12) | 2 |
| C | `house_number` 🔗 | String | ✅ | เลขบ้าน (→ Housing.display_number) | บ้าน 1-3 |
| D | `resident_name` | String | | ชื่อผู้พัก | สมชาย ใจดี |
| E | `prev_meter` | Number | | เลขมิเตอร์น้ำก่อน | 1250 |
| F | `curr_meter` | Number | | เลขมิเตอร์น้ำปัจจุบัน | 1275 |
| G | `water_amount` | Number | ✅ | ค่าน้ำ (บาท) | 125 |
| H | `electric_amount` | Number | ✅ | ค่าไฟ (บาท) | 450.50 |
| I | `common_fee` | Number | ✅ | ค่าส่วนกลาง (บาท) | 110 |
| J | `total_amount` | Number | ✅ | ยอดรวม (น้ำ+ไฟ+ส่วนกลาง) | 685.50 |
| K | `is_exempt` | Boolean | | ยกเว้นค่าส่วนกลาง: TRUE/FALSE | FALSE |
| L | `due_date` 📅 | Date | ✅ | วันครบกำหนดชำระ | 2569-02-20 |
| M | `saved_at` 📅 | DateTime | ✅ | วันที่บันทึก snapshot | 2569-02-01 10:00:00 |
| N | `saved_by` | String | | ผู้บันทึก | team@ppk.ac.th |

**การสร้างแผ่นงานปีใหม่อัตโนมัติ:**
- เมื่อเรียก `saveNotificationSnapshot(period, data)` แล้วไม่พบแผ่นงานของปีนั้น → เรียก `createNotificationYearSheet(year)` สร้างแผ่นงานใหม่พร้อม Header อัตโนมัติ

> **หมายเหตุ HTML:** `payment-notification.html` ปัจจุบันใช้ `getSampleData()` (hardcoded) — Backend ต้อง replace ด้วยข้อมูลจริงจาก `getBillSummaryAll()` และเมื่อกดบันทึกรูป ต้องเรียก `saveNotificationSnapshot()` เพื่อบันทึก snapshot ลง sheet นี้ด้วย

**ไฟล์ .gs ที่ใช้:** `Billing.gs` — `saveNotificationSnapshot()`, `getNotificationHistory()`, `createNotificationYearSheet()`  
**HTML ที่เรียก:** `payment-notification.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~50-100 แถว/เดือน × 12 = ~600-1,200 แถว/ปี (ต่อแผ่นงาน)

---

### 📄 ไฟล์ที่ 5: `[PAYMENTS] การชำระเงิน`

**ตัวแปร ID:** `SPREADSHEET_IDS.PAYMENTS`  
**ไฟล์ .gs ที่ใช้:** `Payment.gs`  
**วัตถุประสงค์:** เก็บข้อมูลการส่งสลิป การชำระเงิน และยอดค้างชำระ

> **หลักการ:**
> 1. SlipSubmissions + PaymentHistory ใช้แผ่นงานแยกตามปี พ.ศ. (เช่น `SlipSubmissions_2569`, `PaymentHistory_2569`...) — สร้างอัตโนมัติเมื่อขึ้นปีใหม่
> 2. Outstanding เป็นแผ่นงานตายตัว ไม่แยกปี — เพราะเป็นข้อมูล "สด" ที่เปลี่ยนตลอด (ลบเมื่อจ่ายครบ)
> 3. ทุกรายการมี `submitted_at` / `payment_date` (วันเดือนปีเวลา) กำกับเสมอ

---

#### แผ่นงานตามปี: `SlipSubmissions_{ปี พ.ศ.}` เช่น `SlipSubmissions_2569` — การส่งสลิป

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสสลิป | SLP1708012345678_U1V2 |
| B | `month` | Number | ✅ | เดือน (1-12) | 2 |
| C | `house_number` 🔗 | String | ✅ | เลขบ้าน | บ้าน 1-3 |
| D | `resident_name` | String | | ชื่อผู้พัก | สมชาย ใจดี |
| E | `email` | String | | อีเมลผู้ส่ง | somchai@email.com |
| F | `notified_amount` | Number | | ยอดที่แจ้ง (น้ำ+ไฟ+ส่วนกลาง) | 685.50 |
| G | `paid_amount` | Number | ✅ | ยอดที่จ่าย | 700 |
| H | `slip_file_ids` | String | ✅ | รหัสไฟล์รูปสลิปใน Drive (คั่นด้วย ,) | abc123,def456 |
| I | `status` | String | ✅ | สถานะ: `pending`/`approved`/`rejected`/`match`/`mismatch` | pending |
| J | `payment_method` | String | | วิธีชำระ: `slip` / `cash` / `manual` | slip |
| K | `is_manual` | Boolean | | บันทึกมือ: TRUE (เจ้าหน้าที่บันทึกเอง) | FALSE |
| L | `reviewed_by` | String | | ผู้ตรวจ | team@ppk.ac.th |
| M | `reviewed_at` 📅 | DateTime | | วันที่ตรวจ | 2569-02-16 14:30:00 |
| N | `review_note` | String | | หมายเหตุการตรวจ | ยอดตรง อนุมัติ |
| O | `submitted_at` 📅 | DateTime | ✅ | วันที่ส่ง | 2569-02-10 18:00:00 |

> **หมายเหตุ HTML:**
> - `upload-slip.html` บันทึกสลิปเป็น base64 ใน localStorage: `{ unitNumber, name, periodKey, notifiedAmount, paidAmount, slipImages: [base64...], submittedAt, status: 'pending' }` — Backend ต้องแปลง base64 → บันทึกไฟล์ลง Drive → เก็บ file ID ใน `slip_file_ids`
> - `check-slip.html` อนุมัติรูปแบบ: `{ status: 'approved', approvedAt, approvedBy, paidAmount, notifiedAmount, paymentMethod, isManual }` — ใช้สถานะ: `match`, `mismatch`, `pending`, `unpaid`, `approved`, `no-resident`
> - `check-slip.html` มีฟังก์ชัน "บันทึกรับเงินสดมือ" (manual payment) → ตั้ง `is_manual = TRUE`
> - `upload-slip.html` อ่าน `commonFeeRate` จาก localStorage — แต่ `admin-settings.html` เขียนเป็น `commonFee` ⚠️ Backend ต้องรองรับทั้งสอง key

**การสร้างแผ่นงานปีใหม่อัตโนมัติ:**
- เมื่อเรียก `handleSubmitSlip(data)` หรือ `getSlipSubmissions(period)` แล้วไม่พบแผ่นงาน `SlipSubmissions_{year}` → เรียก `createSlipYearSheet(year)` สร้างแผ่นงานใหม่พร้อม Header อัตโนมัติ

**ไฟล์ .gs ที่ใช้:** `Payment.gs` — `handleSubmitSlip()`, `getSlipSubmissions()`, `getSlipDetail()`, `handleReviewSlip()`, `createSlipYearSheet()`  
**HTML ที่เรียก:** `upload-slip.html`, `check-slip.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~50-100 แถว/เดือน × 12 = ~600-1,200 แถว/ปี (ต่อแผ่นงาน)

> **หมายเหตุ:** คอลัมน์ `year` ถูกตัดออกเนื่องจากแผ่นงานแยกตามปีแล้ว — ปีของข้อมูลระบุโดยชื่อแผ่นงาน (เช่น `SlipSubmissions_2569`)

---

#### แผ่นงานตามปี: `PaymentHistory_{ปี พ.ศ.}` เช่น `PaymentHistory_2569` — ประวัติการชำระ

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสประวัติ | PAY1708012345678_W3X4 |
| B | `month` | Number | ✅ | เดือน (1-12) | 2 |
| C | `house_number` 🔗 | String | ✅ | เลขบ้าน | บ้าน 1-3 |
| D | `resident_name` | String | | ชื่อผู้พัก | สมชาย ใจดี |
| E | `water_amount` | Number | | ค่าน้ำ | 125 |
| F | `electric_amount` | Number | | ค่าไฟ | 450.50 |
| G | `common_fee` | Number | | ค่าส่วนกลาง | 110 |
| H | `total_amount` | Number | ✅ | ยอดรวม | 685.50 |
| I | `paid_amount` | Number | ✅ | ยอดที่จ่าย | 700 |
| J | `payment_date` 📅 | Date | ✅ | วันที่ชำระ | 2569-02-10 |
| K | `slip_id` 🔗 | String | | รหัสสลิป (→ SlipSubmissions_{year}.id) / manual | SLP1708012345678_U1V2 |
| L | `status` | String | ✅ | สถานะ: `paid` / `partial` / `overpaid` | paid |

**การสร้างแผ่นงานปีใหม่อัตโนมัติ:**
- เมื่อเรียก `getPaymentHistory(period)` หรือ `handleReviewSlip()` / `recordManualPayment()` แล้วไม่พบแผ่นงาน `PaymentHistory_{year}` → เรียก `createPaymentHistoryYearSheet(year)` สร้างแผ่นงานใหม่พร้อม Header อัตโนมัติ

**ไฟล์ .gs ที่ใช้:** `Payment.gs` — `getPaymentHistory()`, `createPaymentHistoryYearSheet()`, ถูกสร้างอัตโนมัติเมื่อ `handleReviewSlip()` หรือ `recordManualPayment()`  
**HTML ที่เรียก:** `payment-history.html`, `dashboard.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~50-100 แถว/เดือน × 12 = ~600-1,200 แถว/ปี (ต่อแผ่นงาน)

> **หมายเหตุ:** คอลัมน์ `year` ถูกตัดออกเนื่องจากแผ่นงานแยกตามปีแล้ว — ปีของข้อมูลระบุโดยชื่อแผ่นงาน (เช่น `PaymentHistory_2569`)

> **หมายเหตุ HTML:** `payment-history.html` ปัจจุบันแสดง **ข้อมูล hardcoded** ในตาราง HTML (3 เดือนตัวอย่าง) — ไม่ได้อ่านจาก localStorage หรือ Backend เลย — Backend ต้องเพิ่มการโหลดข้อมูลจริงจาก PaymentHistory_{year} sheet

---

#### แผ่นงานตายตัว: `Outstanding` — ยอดค้างชำระ

> **หมายเหตุ:** Outstanding เป็นแผ่นงานตายตัว ไม่แยกตามปี — เพราะเป็นข้อมูล "สด" ที่เปลี่ยนตลอด (ลบเมื่อจ่ายครบ)

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัส | OUT1708012345678_Y5Z6 |
| B | `house_number` 🔗 | String | ✅ | เลขบ้าน | บ้าน 1-3 |
| C | `resident_name` | String | | ชื่อผู้พัก | สมชาย ใจดี |
| D | `year` | Number | ✅ | ปีที่ค้าง | 2569 |
| E | `month` | Number | ✅ | เดือนที่ค้าง | 1 |
| F | `water_amount` | Number | | ค่าน้ำค้าง | 125 |
| G | `electric_amount` | Number | | ค่าไฟค้าง | 450.50 |
| H | `common_fee` | Number | | ค่าส่วนกลางค้าง | 110 |
| I | `total_due` | Number | ✅ | ยอดค้างรวม | 685.50 |
| J | `paid_amount` | Number | | ยอดที่จ่ายแล้ว | 0 |
| K | `balance` | Number | ✅ | ยอดคงเหลือ (total_due - paid_amount) | 685.50 |
| L | `last_updated` 📅 | DateTime | ✅ | อัปเดตล่าสุด | 2569-02-16 14:30:00 |

**ไฟล์ .gs ที่ใช้:** `Payment.gs` — `getOutstanding()`, `updateOutstanding()`  
**HTML ที่เรียก:** `check-slip.html`, `dashboard.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~0-50 แถว (เฉพาะบ้านที่ค้าง ลบเมื่อจ่ายครบ)

---

### 📄 ไฟล์ที่ 6: `[REQUESTS] คำร้อง`

**ตัวแปร ID:** `SPREADSHEET_IDS.REQUESTS`  
**ไฟล์ .gs ที่ใช้:** `Request.gs`  
**วัตถุประสงค์:** เก็บคำร้อง 4 ประเภท (เข้าพัก ย้าย คืน ซ่อม) + ระบบคิวรอเข้าพัก — แผ่นงานแยกตามปี พ.ศ.

> **หลักการ:**
> 1. คำร้องทั้ง 4 ประเภทใช้แผ่นงานแยกตามปี พ.ศ. (เช่น `Residence_2569`, `Repair_2570`...) — สร้างอัตโนมัติเมื่อขึ้นปีใหม่
> 2. Queue (คิวรอเข้าพัก) เป็นแผ่นงานตายตัว ไม่แยกปี — เพราะเป็นข้อมูล "สด" ที่เปลี่ยนตลอด (Drag & Drop, หมดอายุ)
> 3. ทุกคำร้องมี `submitted_at` (วันเดือนปีที่ยื่น) กำกับเสมอ — ปีของข้อมูลระบุโดยชื่อแผ่นงาน
> 4. `check-request.html` ดึงข้อมูลตามปีที่เลือก จากแผ่นงานปีนั้นๆ

---

#### แผ่นงานตามปี: `Residence_{ปี พ.ศ.}` เช่น `Residence_2569` — คำร้องขอเข้าพัก

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสคำร้อง | REQ1708012345678_A1B2 |
| B | `submitted_at` 📅 | DateTime | ✅ | วันเดือนปีที่ยื่นคำร้อง | 2569-02-10 09:00:00 |
| C | `prefix` | String | ✅ | คำนำหน้า | นาย |
| D | `fullname` | String | ✅ | ชื่อ-นามสกุล | สมชาย ใจดี |
| E | `address` | String | ✅ | ที่อยู่ปัจจุบัน | 123/4 ถ.ซุปเปอร์ไฮเวย์ อ.เมือง จ.พะเยา |
| F | `phone` | String | ✅ | เบอร์โทร | 081-234-5678 |
| G | `email` | String | | อีเมล | somchai@email.com |
| H | `line_id` | String | | LINE ID | somchai_line |
| I | `subject_group` | String | ✅ | กลุ่มสาระ | วิทยาศาสตร์ |
| J | `stay_type` | String | ✅ | ประเภทการพัก: `alone` (อยู่คนเดียว) / `family` (อยู่กับครอบครัว) | alone |
| K | `total_residents` | Number | ✅ | จำนวนผู้พักอาศัยทั้งหมด | 3 |
| L | `residents_json` | String | | รายชื่อผู้ร่วมพัก (JSON array) | [{"prefix":"นาง","name":"สมหญิง","relation":"คู่สมรส"}] |
| M | `reasons` | String | ✅ | เหตุผล (คั่นด้วย ,) | new_position,no_nearby_residence |
| N | `additional_info` | String | | ข้อมูลเพิ่มเติม | ต้องการบ้านชั้นเดียว |
| O | `documents` | String | | เอกสารที่แนบ (checkbox, คั่นด้วย ,) | สำเนาบัตร,ทะเบียนบ้าน |
| P | `attachment_ids` | String | | รหัสไฟล์แนบใน Drive (คั่นด้วย ,) | file_id_1,file_id_2 |
| Q | `status` | String | ✅ | สถานะ: `pending`/`reviewing`/`waiting`/`approved`/`rejected`/`completed`/`expired` | pending |
| R | `queue_position` | Number | | ลำดับคิว (ถ้าอยู่ในคิว) | 3 |
| S | `expiry_date` 📅 | Date | | วันหมดอายุคิว | 2569-08-10 |
| T | `reviewed_by` | String | | ผู้ตรวจ | admin@ppk.ac.th |
| U | `reviewed_at` 📅 | DateTime | | วันที่ตรวจ | 2569-02-16 14:30:00 |
| V | `review_note` | String | | หมายเหตุการตรวจ | อนุมัติ รอคิว |

> **หมายเหตุ HTML:**
> - `request-form.html` ใช้ `stay_type`: `alone` / `family` (ไม่ใช่ `house`/`flat`)
> - `request-form.html` ค่า `reason`: `new_position`, `far_from_home`, `family_responsibility`, `no_nearby_residence`, `other`
> - `request-form.html` ฟิลด์ `resident_relation[]`: คู่สมรส, บุตร, บิดา, มารดา
> - `check-request.html` ข้อมูลใช้ `{ id, submitDate, prefix, fullname, phone, email, lineId, subjectGroup, stayType, totalResidents, residents[], reasons[], additionalInfo, attachments[], status, queuePosition, expiryDate, notes }`
> - `check-request.html` มีสถานะเพิ่ม: `reviewing`, `waiting`, `completed`, `expired` — นอกเหนือจาก pending/approved/rejected
> - ⚠️ ปัจจุบัน `request-form.html` ใช้ `printForm()` เท่านั้น ไม่มีการ submit — Backend ต้องเพิ่ม

**การสร้างแผ่นงานปีใหม่อัตโนมัติ:**
- เมื่อเรียก `handleSubmitRequest('residence', ...)` หรือ `getRequests('residence', year)` แล้วไม่พบแผ่นงาน `Residence_{year}` → เรียก `createRequestYearSheet('residence', year)` สร้างแผ่นงานใหม่พร้อม Header อัตโนมัติ

**ไฟล์ .gs ที่ใช้:** `Request.gs` — `handleSubmitRequest('residence', ...)`, `getRequests('residence', year)`, `getRequestDetail()`, `handleReviewRequest()`, `createRequestYearSheet('residence', year)`  
**HTML ที่เรียก:** `request-form.html`, `check-request.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~10-50 แถว/ปี (ต่อแผ่นงาน)

---

#### แผ่นงานตามปี: `Transfer_{ปี พ.ศ.}` เช่น `Transfer_2569` — คำร้องขอย้าย

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสคำร้อง | TRF1708012345678_C3D4 |
| B | `submitted_at` 📅 | DateTime | ✅ | วันเดือนปีที่ยื่นคำร้อง | 2569-02-10 09:00:00 |
| C | `prefix` | String | ✅ | คำนำหน้า | นาง |
| D | `fullname` | String | ✅ | ชื่อ-นามสกุล | สมหญิง รักดี |
| E | `phone` | String | ✅ | เบอร์โทร | 089-876-5432 |
| F | `subject_group` | String | ✅ | กลุ่มสาระ | ภาษาไทย |
| G | `current_house` 🔗 | String | ✅ | บ้านปัจจุบัน | บ้าน 1-3 |
| H | `transfer_type` | String | ✅ | ประเภทย้าย: `flat_to_house` / `house_to_flat` / `change_unit` | flat_to_house |
| I | `target_house` | String | | บ้านที่ต้องการย้ายไป | แฟลต 2-5 |
| J | `reasons` | String | ✅ | เหตุผล (คั่นด้วย ,) | family_space,health_issue |
| K | `additional_info` | String | | ข้อมูลเพิ่มเติม | ต้องการห้องชั้นล่าง |
| L | `attachment_ids` | String | | รหัสไฟล์แนบ | file_id_1 |
| M | `status` | String | ✅ | สถานะ | pending |
| N | `reviewed_by` | String | | ผู้ตรวจ | admin@ppk.ac.th |
| O | `reviewed_at` 📅 | DateTime | | วันที่ตรวจ | 2569-02-16 14:30:00 |
| P | `review_note` | String | | หมายเหตุ | อนุมัติ |

> **หมายเหตุ HTML:**
> - `transfer-form.html` ใช้ `transfer_type`: `flat_to_house` / `house_to_flat` / `change_unit` (ไม่ใช่ `swap`/`move`)
> - `transfer-form.html` ค่า `reason`: `family_space`, `family_responsibility`, `health_issue`, `other`
> - `check-request.html` ข้อมูลใช้ `{ id, submitDate, prefix, fullname, phone, subjectGroup, currentHouse, requestedHouse, transferReason, attachments[], status, notes }`
> - ⚠️ ปัจจุบัน `transfer-form.html` ใช้ `printForm()` เท่านั้น ไม่มีการ submit

**การสร้างแผ่นงานปีใหม่อัตโนมัติ:**
- เมื่อเรียก `handleSubmitRequest('transfer', ...)` หรือ `getRequests('transfer', year)` แล้วไม่พบแผ่นงาน `Transfer_{year}` → เรียก `createRequestYearSheet('transfer', year)` สร้างแผ่นงานใหม่พร้อม Header อัตโนมัติ

**ไฟล์ .gs ที่ใช้:** `Request.gs` — `handleSubmitRequest('transfer', ...)`, `getRequests('transfer', year)`, `createRequestYearSheet('transfer', year)`  
**HTML ที่เรียก:** `transfer-form.html`, `check-request.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~5-20 แถว/ปี (ต่อแผ่นงาน)

---

#### แผ่นงานตามปี: `Return_{ปี พ.ศ.}` เช่น `Return_2569` — คำร้องขอคืนบ้านพัก

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสคำร้อง | RTN1708012345678_E5F6 |
| B | `submitted_at` 📅 | DateTime | ✅ | วันเดือนปีที่ยื่นคำร้อง | 2569-02-10 09:00:00 |
| C | `prefix` | String | ✅ | คำนำหน้า | นาย |
| D | `fullname` | String | ✅ | ชื่อ-นามสกุล | สมศักดิ์ มั่นคง |
| E | `phone` | String | ✅ | เบอร์โทร | 086-123-4567 |
| F | `subject_group` | String | ✅ | กลุ่มสาระ | คณิตศาสตร์ |
| G | `current_house` 🔗 | String | ✅ | บ้านที่คืน | บ้าน 3-2 |
| H | `move_in_date` 📅 | Date | | วันที่ย้ายเข้า | 2567-06-01 |
| I | `return_date` 📅 | Date | ✅ | วันที่ต้องการคืน | 2569-03-31 |
| J | `reasons` | String | ✅ | เหตุผล (คั่นด้วย ,) | transfer,resign |
| K | `additional_info` | String | | ข้อมูลเพิ่มเติม | จะส่งมอบวันที่ 31 มี.ค. |
| L | `attachment_ids` | String | | รหัสไฟล์แนบ | file_id_1 |
| M | `status` | String | ✅ | สถานะ | pending |
| N | `reviewed_by` | String | | ผู้ตรวจ | admin@ppk.ac.th |
| O | `reviewed_at` 📅 | DateTime | | วันที่ตรวจ | 2569-02-16 14:30:00 |
| P | `review_note` | String | | หมายเหตุ | อนุมัติ |

> **หมายเหตุ HTML:**
> - `return-form.html` ค่า `reason`: `transfer`, `resign`, `disqualified`, `mou_violation`, `revoked`, `other`
> - `check-request.html` ข้อมูลใช้ `{ id, submitDate, prefix, fullname, phone, subjectGroup, houseNumber, returnDate, returnReason, attachments[], status, notes }`
> - ⚠️ ปัจจุบัน `return-form.html` ใช้ `printForm()` เท่านั้น ไม่มีการ submit

**การสร้างแผ่นงานปีใหม่อัตโนมัติ:**
- เมื่อเรียก `handleSubmitRequest('return', ...)` หรือ `getRequests('return', year)` แล้วไม่พบแผ่นงาน `Return_{year}` → เรียก `createRequestYearSheet('return', year)` สร้างแผ่นงานใหม่พร้อม Header อัตโนมัติ

**ไฟล์ .gs ที่ใช้:** `Request.gs` — `handleSubmitRequest('return', ...)`, `getRequests('return', year)`, `createRequestYearSheet('return', year)`  
**HTML ที่เรียก:** `return-form.html`, `check-request.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~5-15 แถว/ปี (ต่อแผ่นงาน)

---

#### แผ่นงานตามปี: `Repair_{ปี พ.ศ.}` เช่น `Repair_2569` — คำร้องแจ้งซ่อม

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสคำร้อง | RPR1708012345678_G7H8 |
| B | `submitted_at` 📅 | DateTime | ✅ | วันเดือนปีที่ยื่นคำร้อง | 2569-02-10 09:00:00 |
| C | `prefix` | String | ✅ | คำนำหน้า | นาง |
| D | `fullname` | String | ✅ | ชื่อ-นามสกุล | สมใจ ดีงาม |
| E | `house_number` 🔗 | String | ✅ | เลขบ้าน | แฟลต 1-8 |
| F | `phone` | String | ✅ | เบอร์โทร | 082-345-6789 |
| G | `subject_group` | String | | กลุ่มสาระ | ศิลปะ |
| H | `work_types` | String | ✅ | ประเภทงาน (คั่นด้วย ,) | plumbing,electrical |
| I | `problem_detail` | String | ✅ | รายละเอียดปัญหา | ท่อน้ำรั่วในห้องน้ำ |
| J | `urgency` | String | ✅ | ความเร่งด่วน: `normal`/`urgent`/`urgent_high` | urgent |
| K | `cost_responsibility` | String | ✅ | ผู้รับผิดชอบค่าใช้จ่าย: `self`/`school` | school |
| L | `cost_items_json` | String | | รายการค่าใช้จ่าย (JSON) | [{"item":"ท่อ PVC","price":250}] |
| M | `total_cost` | Number | | ค่าใช้จ่ายรวม | 250 |
| N | `attachment_ids` | String | | รหัสไฟล์แนบ | file_id_1,file_id_2 |
| O | `status` | String | ✅ | สถานะ | pending |
| P | `reviewed_by` | String | | ผู้ตรวจ | admin@ppk.ac.th |
| Q | `reviewed_at` 📅 | DateTime | | วันที่ตรวจ | 2569-02-16 14:30:00 |
| R | `review_note` | String | | หมายเหตุ | ส่งช่างซ่อมแล้ว |

> **หมายเหตุ HTML:**
> - `repair-form.html` ค่า `work_type`: `electrical`, `plumbing`, `structure`, `fixtures`, `utilities`, `other`
> - `repair-form.html` ค่า `urgency`: `urgent_high` (เร่งด่วนมาก), `urgent` (เร่งด่วน), `normal` (ปกติ) — ไม่ใช่ `critical`
> - `repair-form.html` ค่า `cost_responsibility`: `self` (ผู้พักจ่ายเอง), `school` (โรงเรียนจ่าย) — ไม่ใช่ `resident`
> - `check-request.html` ข้อมูลใช้ `{ id, submitDate, prefix, fullname, phone, houseNumber, subjectGroup, workTypes[], problemDetail, urgency, attachments[], status, notes }`
> - ⚠️ ปัจจุบัน `repair-form.html` ใช้ `printForm()` เท่านั้น ไม่มีการ submit

**การสร้างแผ่นงานปีใหม่อัตโนมัติ:**
- เมื่อเรียก `handleSubmitRequest('repair', ...)` หรือ `getRequests('repair', year)` แล้วไม่พบแผ่นงาน `Repair_{year}` → เรียก `createRequestYearSheet('repair', year)` สร้างแผ่นงานใหม่พร้อม Header อัตโนมัติ

**ไฟล์ .gs ที่ใช้:** `Request.gs` — `handleSubmitRequest('repair', ...)`, `getRequests('repair', year)`, `createRequestYearSheet('repair', year)`  
**HTML ที่เรียก:** `repair-form.html`, `check-request.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~10-50 แถว/ปี (ต่อแผ่นงาน)

---

#### แผ่นงานตายตัว: `Queue` — คิวรอเข้าพัก

> **หมายเหตุ:** Queue เป็นแผ่นงานตายตัว ไม่แยกตามปี — เพราะเป็นข้อมูล "สด" ที่เปลี่ยนตลอด (Drag & Drop, หมดอายุ, ย้ายเข้าบ้าน)

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสคิว | QUE1708012345678_I9J0 |
| B | `request_id` 🔗 | String | ✅ | รหัสคำร้อง (→ ResidenceRequests.id) | REQ1708012345678_A1B2 |
| C | `fullname` | String | ✅ | ชื่อ-นามสกุล | สมชาย ใจดี |
| D | `email` | String | | อีเมล | somchai@email.com |
| E | `request_type` | String | ✅ | ประเภทที่พักที่ขอ: `house` / `flat` (ผู้ดูแลกำหนด) | house |
| F | `position` | Number | ✅ | ลำดับคิว | 1 |
| G | `submitted_at` 📅 | DateTime | ✅ | วันที่ยื่นคำร้อง | 2569-02-10 09:00:00 |
| H | `status` | String | ✅ | สถานะ: `waiting`/`called`/`expired`/`approved` | waiting |
| I | `expiry_date` 📅 | Date | | วันหมดอายุคิว | 2569-08-10 |

**ไฟล์ .gs ที่ใช้:** `Request.gs` — `getQueue()`, `addToQueue()`, `removeFromQueue()`, `updateQueueOrder()`, `approveFromQueue()`, `setQueueExpiryDate()`, `checkAndExpireQueue()`  
**HTML ที่เรียก:** `check-request.html` (Drag & Drop จัดลำดับคิว)  
**ปริมาณข้อมูลโดยประมาณ:** ~0-20 แถว (คิวสด)

> **หมายเหตุ HTML:** `check-request.html` จัดเก็บข้อมูลคิวเป็น property ฝังใน request object (`queuePosition`, `expiryDate`) ไม่ได้แยก sheet — Backend ใช้ Queue sheet แยกเพื่อความยืดหยุ่น แต่ต้อง sync `queue_position` + `expiry_date` กลับไปที่ `Residence_{year}` ด้วย

---

### 📄 ไฟล์ที่ 7: `[ACCOUNTING] บัญชี`

**ตัวแปร ID:** `SPREADSHEET_IDS.ACCOUNTING`  
**ไฟล์ .gs ที่ใช้:** `Finance.gs`  
**วัตถุประสงค์:** เก็บบัญชีรายรับ-รายจ่ายกองทุนบ้านพักครู **แยกต่างหากจากข้อมูลอื่น** — แผ่นงานแยกตามปี พ.ศ.

> **หลักการ:**
> 1. ชีทนี้เก็บเฉพาะบัญชีรายรับ-รายจ่ายเท่านั้น ไม่ปนกับข้อมูลอื่น
> 2. แผ่นงานแยกตามปี พ.ศ. (เช่น `2569`, `2570`...) — สร้างอัตโนมัติเมื่อขึ้นปีใหม่
> 3. ข้อมูลที่ดึงจากหน้าอื่น (ค่าส่วนกลาง, ค่าไฟปัดเศษ, ค่าขยะ) จะถูก **คัดลอก** มาบันทึกในชีทนี้ ไม่ต้องย้อนไปอ่านจากต้นทางอีก
> 4. รูปหลักฐานที่แนบจะบันทึกลงโฟลเดอร์ `AccountingReceipts/` บน Drive

---

#### แผ่นงานตามปี: `{ปี พ.ศ.}` เช่น `2569` — บัญชีรายรับ-รายจ่ายประจำปี

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัสรายการ (INC หรือ EXP + timestamp) | INC1708012345678_K1L2 |
| B | `month` | Number | ✅ | เดือน (1-12) | 2 |
| C | `type` | String | ✅ | ประเภท: `income` (รายรับ) / `expense` (รายจ่าย) | income |
| D | `date` 📅 | Date | ✅ | วันที่เกิดรายการ | 2569-02-15 |
| E | `description` | String | ✅ | รายละเอียด | รับค่าส่วนกลาง เดือน ก.พ. |
| F | `category` | String | ✅ | หมวด (ดูตารางด้านล่าง) | common_fee |
| G | `amount` | Number | ✅ | จำนวนเงิน (บาท) | 5,500 |
| H | `source` | String | ✅ | แหล่งที่มา: `auto` (ดึงอัตโนมัติ) / `manual` (กรอกมือ) | auto |
| I | `source_ref` | String | | อ้างอิงต้นทาง (หน้าที่ดึงมา / เลขที่เอกสาร) | payment-notification / REC-2569-02-001 |
| J | `receipt_file_id` | String | | รหัสไฟล์รูปหลักฐานบน Drive | file_id_abc123 |
| K | `note` | String | | หมายเหตุ | 50 หน่วย × 110 บาท |
| L | `carry_forward` | Number | | ยอดยกมา (เฉพาะแถวแรกของเดือน) | 4,250 |
| M | `added_by` | String | | ผู้บันทึก | accounting@ppk.ac.th |
| N | `added_at` 📅 | DateTime | ✅ | วันที่บันทึก | 2569-02-15 16:00:00 |

**หมวดรายรับ (category สำหรับ type=income):**

| category | คำอธิบาย | แหล่งที่ดึง (source=auto) |
|----------|----------|------------------------|
| `common_fee` | ค่าส่วนกลาง | หน้าแจ้งยอดชำระ (`payment-notification.html`) |
| `electric_rounding` | ค่าไฟปัดเศษ (ส่วนต่างจากการปัดขึ้น) | หน้าบันทึกค่าไฟ (`record-electric.html`) |
| `other_income` | รายรับอื่นๆ | กรอกมือ (source=manual) |

**หมวดรายจ่าย (category สำหรับ type=expense):**

| category | คำอธิบาย | แหล่งที่ดึง (source=auto) |
|----------|----------|------------------------|
| `garbage` | ค่าขยะ | หน้ายอดเบิกประจำเดือน (`monthly-withdraw.html`) |
| `additional_withdraw` | ค่าใช้จ่ายเพิ่มเติมจากยอดเบิก | หน้ายอดเบิกประจำเดือน (`monthly-withdraw.html`) |
| `repair` | ค่าซ่อมแซม | กรอกมือ (source=manual) |
| `supplies` | ค่าวัสดุ | กรอกมือ (source=manual) |
| `other_expense` | รายจ่ายอื่นๆ | กรอกมือ (source=manual) |

**การตั้งชื่อรูปหลักฐาน:**
- ชื่อไฟล์: `{description}_{YYYY-MM-DD}.jpg` เช่น `ค่าส่วนกลาง_2569-02-15.jpg`
- บันทึกลงโฟลเดอร์: `AccountingReceipts/`

**การสร้างแผ่นงานปีใหม่อัตโนมัติ:**
- เมื่อเรียก `loadAccountingData(period)` แล้วไม่พบแผ่นงานของปีนั้น → เรียก `createYearSheet(year)` สร้างแผ่นงานใหม่พร้อม Header อัตโนมัติ
- ยอดยกมา (`carry_forward`) ของเดือนแรกของปีใหม่ = ยอดคงเหลือสิ้นเดือนสุดท้ายของปีก่อน

**การดึงข้อมูลอัตโนมัติ (Auto Entries):**
| ข้อมูล | ดึงจาก | วิธีดึง |
|--------|--------|--------|
| รายรับ: ค่าส่วนกลาง | หน้าแจ้งยอดชำระ (`payment-notification`) | ดึงยอดรวมค่าส่วนกลางที่บันทึกไว้แล้ว |
| รายรับ: ค่าไฟปัดเศษ | หน้าบันทึกค่าไฟ (`record-electric`) | ผลรวม (ค่าไฟปัดเศษทุกบ้าน - ค่าไฟจริงทุกบ้าน) |
| รายจ่าย: ค่าขยะ | หน้ายอดเบิกประจำเดือน (`monthly-withdraw`) | ดึง `garbageFee` |
| รายจ่าย: ค่าอื่นๆ จากยอดเบิก | หน้ายอดเบิกประจำเดือน (`monthly-withdraw`) | ดึง `additionalItems[]` |

> **หมายเหตุ:** ข้อมูลทั้งที่ดึงอัตโนมัติและกรอกมือ จะถูก **บันทึกลงในชีท [ACCOUNTING] เท่านั้น** — เป็น snapshot สมบูรณ์ของเดือนนั้น ไม่ต้องย้อนกลับไปอ่านจากต้นทาง

> **หมายเหตุ HTML:**
> - `accounting.html` บันทึกรูปแบบ `{ carryForward, incomeItems: [{ date, description, amount, note, auto, image }], expenseItems: [...], savedAt }` — รายการ `auto` = รายการที่ดึงอัตโนมัติ
> - `accounting.html` เก็บรูปหลักฐานเป็น base64 — Backend ต้องแปลงเป็นไฟล์บน Drive → เก็บ `receipt_file_id` ในชีท
> - `accounting.html` อ่าน `commonFeeRate` (ไม่ใช่ `commonFee`) — Backend ต้อง return ค่าผ่าน API

**ไฟล์ .gs ที่ใช้:** `Finance.gs` — `loadAccountingData()`, `saveAccountingData()`, `calculateAutoEntries()`, `createYearSheet()`, `getCarryForward()`, `uploadReceiptImage()`  
**HTML ที่เรียก:** `accounting.html`  
**ปริมาณข้อมูลโดยประมาณ:** ~8-35 แถว/เดือน × 12 = ~100-420 แถว/ปี (ต่อแผ่นงาน)

---

### 📄 ไฟล์ที่ 8: `[WITHDRAW] สรุปเบิกจ่าย`

**ตัวแปร ID:** `SPREADSHEET_IDS.WITHDRAW`  
**ไฟล์ .gs ที่ใช้:** `Finance.gs`  
**วัตถุประสงค์:** เก็บข้อมูลสรุปเบิกจ่ายรายเดือน **แยกต่างหากจากข้อมูลอื่น** — แผ่นงานแยกตามปี พ.ศ.

> **หลักการ:**
> 1. ชีทนี้เก็บเฉพาะสรุปเบิกจ่ายรายเดือนเท่านั้น (ข้อมูลต้นทางของยอดเบิก ไม่ใช่บัญชีรายรับ-รายจ่าย)
> 2. แผ่นงานแยกตามปี พ.ศ. (เช่น `2569`, `2570`...) — สร้างอัตโนมัติเมื่อขึ้นปีใหม่
> 3. ข้อมูลจากที่นี่จะถูก **ดึงไปบันทึก** เป็นรายจ่ายในชีท [ACCOUNTING] อีกครั้ง
> 4. แยกเป็นไฟล์ Google Sheets ต่างหากเพื่อให้จัดการสิทธิ์และสำรองข้อมูลได้อิสระ

---

#### แผ่นงานตามปี: `{ปี พ.ศ.}` เช่น `2569` — สรุปเบิกจ่ายประจำปี

| คอลัมน์ | ชื่อ | ประเภทข้อมูล | จำเป็น | คำอธิบาย | ตัวอย่าง |
|---------|------|-------------|--------|----------|---------|
| A | `id` 🔑 | String | ✅ | รหัส | WTD1708012345678_O5P6 |
| B | `month` | Number | ✅ | เดือน (1-12) | 2 |
| C | `water_total` | Number | ✅ | ยอดรวมค่าน้ำทั้งหมด | 6,250 |
| D | `electric_total` | Number | ✅ | ยอด กฟภ. (จาก PEA) | 45,000 |
| E | `common_total` | Number | | ยอดรวมค่าส่วนกลาง | 5,500 |
| F | `garbage_fee` | Number | ✅ | ค่าขยะ | 310 |
| G | `additional_items_json` | String | | รายการค่าใช้จ่ายเพิ่มเติม (JSON) | [{"name":"ค่าซ่อมท่อ","amount":500}] |
| H | `additional_total` | Number | | ยอดรวมค่าใช้จ่ายเพิ่มเติม | 500 |
| I | `withdraw_amount` | Number | ✅ | ยอดเบิกรวม | 52,560 |
| J | `withdraw_date` 📅 | Date | | วันที่เบิก | 2569-02-28 |
| K | `withdraw_by` | String | | ผู้เบิก | finance@ppk.ac.th |
| L | `note` | String | | หมายเหตุ | - |
| M | `saved_at` 📅 | DateTime | ✅ | วันที่บันทึก | 2569-02-28 16:00:00 |
| N | `saved_by` | String | | ผู้บันทึก | team@ppk.ac.th |

**การสร้างแผ่นงานปีใหม่อัตโนมัติ:**
- เมื่อเรียก `getMonthlyWithdraw(period)` แล้วไม่พบแผ่นงานของปีนั้น → เรียก `createWithdrawYearSheet(year)` สร้างแผ่นงานใหม่พร้อม Header อัตโนมัติ

> **หมายเหตุ HTML:** `monthly-withdraw.html` บันทึกรูปแบบ `{ month, year, garbageFee, additionalItems: [{ name, amount }], savedAt }` — มีค่าเริ่มต้น `garbageFee = 310` (ค่าขยะ) + รายการเพิ่มเติมแบบ dynamic — ยอดค่าน้ำ/ค่าไฟดึงจาก localStorage (`waterBill_*`, `electricBill_*`)

**ไฟล์ .gs ที่ใช้:** `Finance.gs` — `getMonthlyWithdraw()`, `handleSaveWithdraw()`, `createWithdrawYearSheet()`  
**HTML ที่เรียก:** `monthly-withdraw.html`  
**ปริมาณข้อมูลโดยประมาณ:** 12 แถว/ปี (เดือนละ 1 แถว ต่อแผ่นงาน)

---

## 📋 4. ตารางสรุป ID ที่ต้องบันทึก

> หลังรัน `setupAll()` ใน setup.gs จะได้ ID เหล่านี้ → นำไปใส่ใน `Config.gs`

### 4.1 Folder IDs (14 โฟลเดอร์)

| # | ตัวแปรใน Config.gs | โฟลเดอร์ | ID (กรอกหลัง setup) |
|---|-------------------|---------|---------------------|
| 1 | `ROOT_FOLDER_ID` | HOME PPK 2026 | `________________` |
| 2 | `DATA_FOLDER_ID` | Data | `________________` |
| 3 | `SLIPS_FOLDER_ID` | Slips | `________________` |
| 4 | `REQUEST_ATTACHMENTS_FOLDER_ID` | RequestAttachments | `________________` |
| 5 | `RESIDENCE_REQ_FOLDER_ID` | ResidenceRequests | `________________` |
| 6 | `TRANSFER_REQ_FOLDER_ID` | TransferRequests | `________________` |
| 7 | `RETURN_REQ_FOLDER_ID` | ReturnRequests | `________________` |
| 8 | `REPAIR_REQ_FOLDER_ID` | RepairRequests | `________________` |
| 9 | `ACCOUNTING_RECEIPTS_FOLDER_ID` | AccountingReceipts | `________________` |
| 10 | `DOCUMENTS_FOLDER_ID` | Documents | `________________` |
| 11 | `BACKUPS_FOLDER_ID` | Backups | `________________` |
| 12 | `SCRIPTS_FOLDER_ID` | Scripts | `________________` |

### 4.2 Spreadsheet IDs (8 ไฟล์)

| # | ตัวแปรใน Config.gs | ชื่อไฟล์ Google Sheets | จำนวนแผ่นงาน | ID (กรอกหลัง setup) |
|---|-------------------|----------------------|-------------|---------------------|
| 1 | `SPREADSHEET_IDS.MAIN` | [MAIN] ฐานข้อมูลหลัก | 10 + PendingReg_{ปี} | `________________` |
| 2 | `SPREADSHEET_IDS.WATER` | [WATER] ค่าน้ำ | ตามปี (เช่น `2569`) | `________________` |
| 3 | `SPREADSHEET_IDS.ELECTRIC` | [ELECTRIC] ค่าไฟ | ตามปี (เช่น `2569`) | `________________` |
| 4 | `SPREADSHEET_IDS.PAYMENTS` | [PAYMENTS] การชำระเงิน | 3 | `________________` |
| 5 | `SPREADSHEET_IDS.REQUESTS` | [REQUESTS] คำร้อง | 4 ประเภท × ตามปี + Queue | `________________` |
| 6 | `SPREADSHEET_IDS.ACCOUNTING` | [ACCOUNTING] บัญชี | ตามปี (เช่น `2569`) | `________________` |
| 7 | `SPREADSHEET_IDS.WITHDRAW` | [WITHDRAW] สรุปเบิกจ่าย | ตามปี (เช่น `2569`) | `________________` |
| 8 | `SPREADSHEET_IDS.NOTIFICATIONS` | [NOTIFICATIONS] แจ้งยอดชำระ | ตามปี (เช่น `2569`) | `________________` |

### 4.3 Web App URL

| รายการ | ค่า (กรอกหลัง Deploy) |
|--------|----------------------|
| Web App URL | `https://script.google.com/macros/s/____________/exec` |
| Deployment ID | `________________` |

---

## 📖 5. คู่มือหลังรัน setup.gs

### ขั้นตอนที่ 1: รัน Setup
1. เปิด [Google Apps Script](https://script.google.com) → สร้าง Project ใหม่
2. Copy เนื้อหาจาก `Scripts/setup.gs` ไปวางใน `Code.gs`
3. กด ▶ Run → เลือก `setupAll()`
4. อนุญาต Permissions (Drive, Sheets)
5. ดู Log → บันทึก ID ทั้งหมดลงตารางข้อ 4

### ขั้นตอนที่ 2: ตรวจสอบโครงสร้าง
- [ ] โฟลเดอร์ 14 โฟลเดอร์ ✅
- [ ] Google Sheets 8 ไฟล์ ✅
- [ ] แผ่นงาน 22+ แผ่นงาน ([MAIN] 10 + PendingReg_{ปี} / [WATER] ตามปี / [ELECTRIC] ตามปี / [PAYMENTS] 3 / [REQUESTS] 4 ประเภท×ตามปี + Queue / [ACCOUNTING] ตามปี / [WITHDRAW] ตามปี / [NOTIFICATIONS] ตามปี) ✅
- [ ] คอลัมน์ Header ถูกต้อง ✅
- [ ] Settings มีค่าเริ่มต้น 46 รายการ (ตรงตาม setup.gs v2.0) ✅
- [ ] WaterRates ว่าง — รอแอดมินกำหนดอัตราค่าน้ำก่อนใช้งาน ✅
- [ ] Slips/2569 มี 12 โฟลเดอร์เดือน ✅
- [ ] RequestAttachments มี 4 ประเภท ✅
- [ ] AccountingReceipts โฟลเดอร์รูปหลักฐานบัญชี ✅
- [ ] [ACCOUNTING] มีแผ่นงานปีปัจจุบัน (2569) ✅
- [ ] [MAIN] WaterRates, CommonFee, Exemptions แผ่นงานที่ 1.8-1.10 ✅
- [ ] [MAIN] PendingReg_2569 แผ่นงานรออนุมัติการสมัคร ✅
- [ ] [WITHDRAW] มีแผ่นงานปีปัจจุบัน (2569) ✅
- [ ] [WATER] มีแผ่นงานปีปัจจุบัน (2569) ✅
- [ ] [ELECTRIC] มีแผ่นงานปีปัจจุบัน (2569) ✅
- [ ] [NOTIFICATIONS] มีแผ่นงานปีปัจจุบัน (2569) ✅
- [ ] [REQUESTS] มีแผ่นงานปีปัจจุบัน (Residence_2569, Transfer_2569, Return_2569, Repair_2569) + Queue ✅

### ขั้นตอนที่ 3: สร้างไฟล์ Backend
1. นำ ID จากตารางข้อ 4 → ใส่ใน `Config.gs`
2. สร้างไฟล์ .gs ตามลำดับใน `แพลนแบคเอ็น.md`
3. ทดสอบทีละไฟล์ตามลำดับ

### ขั้นตอนที่ 4: Deploy Web App
1. Deploy → New deployment → Web app
2. Execute as: Me (ตัวเอง)
3. Who has access: Anyone (ทุกคน)
4. Copy URL → ใส่ในตาราง 4.3

---

## 📊 สรุปภาพรวม

| หมวด | จำนวน |
|------|-------|
| 📁 โฟลเดอร์ Google Drive | 14 โฟลเดอร์ |
| 📊 ไฟล์ Google Sheets | 8 ไฟล์ |
| 📋 แผ่นงาน (Sheets) | 22 แผ่นงาน + แผ่นงานตามปี (MAIN 10 + PendingReg_{ปี} / WATER ตามปี / ELECTRIC ตามปี / PAYMENTS 3 / REQUESTS 4ประเภท×ตามปี + Queue / ACCOUNTING ตามปี / WITHDRAW ตามปี / NOTIFICATIONS ตามปี) |
| 📏 คอลัมน์ทั้งหมด | ~200 คอลัมน์ |
| 🔑 Primary Key | 22+ (ทุกแผ่นงาน) |
| 🔗 Foreign Key | 15 ความสัมพันธ์ |
| ⚙️ ค่าเริ่มต้น Settings | 46 รายการ (ครบทุก key ตาม setup.gs v2.0) |
| 📅 ข้อมูลที่เพิ่มรายเดือน | ~250-600 แถว/เดือน |
| 📅 ข้อมูลที่เพิ่มรายปี | ~3,000-7,000 แถว/ปี |

---

> **เอกสารนี้ใช้ประกอบกับ:** `แพลนแบคเอ็น.md` เพื่อดูว่าฟังก์ชันใดในแต่ละไฟล์ .gs ใช้ข้อมูลจากโฟลเดอร์/ชีท/คอลัมน์ใด

---

## ⚠️ 6. หมายเหตุสำคัญ — ข้อแตกต่างระหว่าง HTML กับ Backend

> **หลักการ:** HTML เป็น source of truth (ไม่แก้ HTML เด็ดขาด) — Backend ต้องปรับให้รองรับ

### 6.1 localStorage Key ที่ไม่ตรงกัน

| ปัญหา | เขียนโดย | อ่านโดย | Backend ต้องทำ |
|--------|---------|--------|---------------|
| `commonFee` vs `commonFeeRate` | admin-settings เขียน `commonFee` | upload-slip + accounting อ่าน `commonFeeRate` | API return ค่าเดียวกัน ไม่ต้องพึ่ง localStorage key |
| `currentUser` vs `currentUserUnit` | - | dashboard/check-slip อ่าน `currentUser` (JSON), upload-slip อ่าน `currentUserUnit` (String) | API return user object ที่มีทั้ง unit + full user data |

### 6.2 หน้าที่ยังไม่ได้ submit ข้อมูล

| HTML | สถานะปัจจุบัน | Backend ต้องเพิ่ม |
|------|-------------|------------------|
| `login.html` | `validateForm()` return true/false เท่านั้น | `google.script.run.handleLogin(email, password)` |
| `register.html` | validate แต่ไม่บันทึก | `google.script.run.handleRegister(data)` → บันทึกลง PendingReg_{ปี} รอแอดมินอนุมัติ |
| `forgot-password.html` | validate เท่านั้น | `google.script.run.handleResetPassword(email)` |
| `forgot-email.html` | validate เท่านั้น | `google.script.run.handleFindEmail(phone)` |
| `request-form.html` | `printForm()` เท่านั้น | `google.script.run.handleSubmitRequest('residence', data)` |
| `transfer-form.html` | `printForm()` เท่านั้น | `google.script.run.handleSubmitRequest('transfer', data)` |
| `return-form.html` | `printForm()` เท่านั้น | `google.script.run.handleSubmitRequest('return', data)` |
| `repair-form.html` | `printForm()` เท่านั้น | `google.script.run.handleSubmitRequest('repair', data)` |

### 6.3 หน้าที่ใช้ข้อมูลตัวอย่าง Hardcoded

| HTML | สถานะ | รายละเอียด |
|------|-------|-----------|
| `payment-history.html` | ❌ Hardcoded | ตาราง HTML มี 3 เดือนตัวอย่าง, กรองด้วย text match — ไม่อ่าน localStorage |
| `payment-notification.html` | ❌ Hardcoded | `getSampleData()` สร้างข้อมูล 5 บ้าน 8 แฟลต — ไม่อ่าน billing data จริง |
| `check-request.html` | ⚠️ Sample init | `initSampleData()` สร้างตัวอย่าง 3 residence, 2 repair, 1 transfer, 1 return — ถ้า localStorage ว่าง |

### 6.4 การจัดเก็บข้อมูลที่ต่างกัน (HTML vs Backend)

| ข้อมูล | HTML เก็บแบบ | Backend ต้องเก็บแบบ |
|--------|------------|-------------------|
| สลิปรูป | base64 ใน localStorage | ไฟล์ใน Drive → เก็บ file ID ใน Sheet |
| รหัสผ่าน | `btoa()` (base64) ใน residentsData | SHA-256 hash ใน Users sheet |
| รูปโปรไฟล์ | base64 ใน userData | base64 ใน Residents sheet หรือ Drive file ID |
| รูปใบเสร็จ | base64 ใน accounting data | ไฟล์ใน Drive (`AccountingReceipts/`) → เก็บ file ID — ตั้งชื่อ `{รายการ}_{YYYY-MM-DD}.jpg` |
| คิวรอเข้าพัก | property ฝังใน request object | Queue sheet แยก + sync กลับ ResidenceRequests |
| Users + Residents | admin-settings รวมเป็น object เดียว | แยก Users sheet + Residents sheet (FK link) |
